version: 2
updates:
  # Keep the SHA-pinned actions in the workflows current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  # Cargo dependencies.
  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: weekly
